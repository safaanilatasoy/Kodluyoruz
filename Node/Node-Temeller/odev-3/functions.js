function circleArea(area){
    return Math.PI * area * area;
}

function circleCircumference(circumference){
    return 2 * Math.PI * circumference;
}

module.exports = {
    circleArea,
    circleCircumference
}